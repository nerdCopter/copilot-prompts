## Git commit policy

- NEVER run `git add` or `git commit` automatically or proactively.
- NEVER stage files or commit changes unless the user explictly commands such.
- Do NOT commit after completeing a task.
- Complete a task does NOT contitute permission to commit.
